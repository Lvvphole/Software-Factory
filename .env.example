# Software Factory v1 — environment template (NO REAL KEYS)
FACTORY_AUTONOMY_LEVEL=2
FACTORY_DEFAULT_PROVIDER=mock
FACTORY_DB_PATH=.factory-memory/factory.db

# Provider credentials — leave blank for v1 dry-run; set in your own .env (gitignored)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
LOCAL_MODEL_ENDPOINT=
