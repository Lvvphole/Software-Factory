# demo-calculator

A tiny package used as a Software Factory `--target-repo` demo. Contains a
deliberately broken `add()` function. A real coding executor (e.g.
claude_code) should fix it; tests should then pass.
